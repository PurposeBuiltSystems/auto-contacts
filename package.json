{
  "name": "auto-contacts",
  "version": "1.1.0",
  "description": "Outlook add-in that adds the people you email to your contacts, with phone/title/company mined from their email signatures.",
  "private": true,
  "scripts": {
    "validate": "office-addin-manifest validate manifest.xml",
    "test": "node tools/check-undeclared.js && node tools/check-pages.js && node test/parser.test.js && node test/summary.test.js && node test/flow.test.js && node test/mobile.test.js",
    "check": "node tools/check-undeclared.js"
  },
  "author": "Matthew Miller",
  "license": "MIT",
  "devDependencies": {
    "office-addin-manifest": "^2.0.4",
    "acorn": "^8.16.0"
  }
}
